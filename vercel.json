{
  "version": 2,
  "buildCommand": "cd mock_amm && yarn install && npx ts-node scripts/setup_invest_apy.ts && cd ../frontend && npm install && npm run build",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs"
}
